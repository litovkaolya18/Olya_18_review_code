#pragma once
#include <string>

//FIX_ME: функция вынесена в заголовочный файл и переименована под Google Style
//bool NoThreeZeros(std::string& num);
// Проверяет, что в числе нет более трёх подряд идущих нулей
bool no_three_zeros(std::string& num);
