 # Лабораторная-1
Вариант - 3
<H2>Исправленные ошибки в коде:</H2>
<A HREF="#chap1">Задание 1 </A><br>
<A HREF="#chap2">Задание 2 </A><br>
<A HREF="#chap3">Задание 3 </A><br>
<A HREF="#chap4">Задание 4 </A><br>

<A NAME="chap1"></A>
<H1>Задание 1 </H1>
<A NAME="chap1.1"></A>
1. Стиль именования (Google Style Guide)<br>
* Имена параметров функции приведены к Google Style (weights, total_weight).<br>
* Неверные имена переменных в объявлении функции (a, w, weight) → заменены на dp, weights, total_weight по Google Style.<br>
* Удалена старая версия объявления функции, оставлена только актуальная.<br>
* Приведено к стилю lower_case_with_underscores<br>
* Приведены комментарии к единому стилю.<br>
* Функция восстановления решения переписана корректно, без лишних переменных и с правильной логикой рекурсии.<br>
* Удалено использование using namespace std<br>
* Удалены слишком длинные строки (>80 символов)<br>
* Удалён ввод N и Z с клавиатуры<br>
<br>
2. Обработка ошибок<br>
* Добавлена проверка: файл не открылся<br>
* Добавлена проверка корректности N и Z<br>
* Удалена генерация случайных весов и цен<br>
* Добавлена проверка корректности весов<br>
* Добавлена проверка корректности ценностей<br>
* Добавлена проверка на лишние данные в файле<br>

<A NAME="chap2"></A>
<H1>Задание 2 </H1>
<A NAME="chap1.2"></A>
1. Стиль именования (Google Style Guide)<br>
* Функции переименованы в lower_case_with_underscores: Solve → solve, Path → build_path.<br>
* Переменные цикла и рабочие переменные переименованы: i, j → row, col. board2 → dp  <br>
(dp — общепринятое имя для динамического программирования).<br>
* Фигурные скобки { перенесены на ту же строку, что и объявление.<br>
* Строки укорочены до ≤80 символов, где это требовалось.<br>
* Удалено using namespace std; Все обращения заменены на std::<br>
<br>
2. Обработка ошибок<br>
* Добавлена проверка корректности n: диапазон 1 < N < 80.<br>
* Добавлена проверка успешного чтения n.<br>
* Добавлена проверка успешного чтения каждого элемента матрицы.<br>
* Добавлена проверка диапазона монет (1..100).<br>
* Добавлены исключения std::runtime_error там, где требуется строгая ошибка.<br>
* Исправлены все места, где использовались старые имена переменных.<br>
* Исправлены вызовы функций после переименования.<br>
 
<A NAME="chap3"></A>
<H1>Задание 3 </H1>
<A NAME="chap1.3"></A>
1. Стиль именования (Google Style Guide)<br>
* Функции переименованы в lower_case_with_underscores: NoThreeZeros → no_three_zeros.<br>
* Переменные получили осмысленные имена: i, j → pos, temp, value и т.д.<br>
* Фигурные скобки { перенесены на ту же строку, что и объявление.<br>
* Строки укорочены до ≤80 символов, где это требовалось.<br>
* Удалено using namespace std; Все обращения заменены на std::<br>
<br>
2. Обработка ошибок<br>
Добавлены защиты:<br>
* Проверка, что пользователь вводит число, а не текст.<br>
* Очистка ошибочного ввода через std::getline()<br>
* Добавлена защита от пустой строки при формировании числа.<br>
* Исправлены вызовы функций после переименования.<br>
