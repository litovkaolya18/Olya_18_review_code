# Лабораторная-2
Вариант - 3
<H2>Исправленные ошибки в коде:</H2>
<A HREF="#chap1">Задание 1 </A><br>
<A HREF="#chap2">Задание 2 </A><br>
<A HREF="#chap3">Задание 3 </A><br>
<A HREF="#chap4">Задание 4 </A><br>

<A NAME="chap1"></A>
<H1>Задание 1 </H1>
<A NAME="chap1.1"></A>
1. Стиль именования (Google Style Guide)<br>
* Удалено using namespace std => Все обращения к стандартной библиотеке приведены к виду std::.<br>
* Исправлены имена классов, функций и переменных => CamelCase, lower_case_with_underscores, lower_case_with_underscores <br>
* Устранены слишком короткие и непонятные имена (d, p1, cnt, num и др.)<br>
* Класс stack переименован в Stack<br>
* Поля структуры Node переименованы (d → data, next → next_)<br>
* Конструктор Node переписан, Использует новые имена полей и корректный стиль.<br>
* Поля класса переименованы (p1 → top_, cnt → count_)<br>
* Методы переименованы (add → add_element, topdel → delete_top, show → show_stack)<br>
* Метод add_element => переменная newnode → new_node, логика переписана под новые имена, корректно обновляет вершину и счётчик<br>
* Метод delete_top => добавлена проверка на пустой стек,
исправлены имена переменных,
удалены старые обращения p1, d, next,
исправлен вывод информации,
удалён код, превышающий 80 символов,
корректное освобождение памяти,
корректное уменьшение счётчика,<br>
* Метод show_stack => переменная temp → temp_node, исправлены обращения к полям узла, логика переписана под новые имена<br>
<br>
2. Обработка ошибок<br>
* Добавлена проверка корректности ввода количества элементов => защита от букв, защита от отрицательных чисел, очистка потока при ошибке<br>
* Добавлена проверка корректности ввода каждого элемента стека => защита от некорректных символов, очистка потока при ошибке<br>
* Добавлена обработка исключений при удалении элемента => Если стек пуст — выводится понятная ошибка.<br>
* Добавлена защита от удаления из пустого стека => Метод delete_top() теперь выбрасывает исключение.<br>
* Исправлено управление памятью => Удаление узлов выполняется корректно.<br>
* Исправлено обновление вершины стека => Используется top_, а не старые переменные.<br>
* Исправлен вывод информации о вершине => Выводятся корректные данные и адреса.<br>
<br>

<A NAME="chap2"></A>
<H1>Задание 2 </H1>
<A NAME="chap1.2"></A>
1. Стиль именования (Google Style Guide)<br>
* Удалено using namespace std => Все обращения к стандартной библиотеке приведены к виду std::<br>
* Приведены к стилю имена классов, методов и переменных => класс Queue — корректный CamelCase,
методы: push, pop, show, even_head, get_p1, get_p2, get_p1_data, get_p2_data — snake_case,
поля: p1_, p2_ — snake_case с подчёркиванием для приватных членов,
параметры функций переименованы в осмысленные (value, input_element, element_count)<br>
* Удалены устаревшие имена (P1, P2, num, inum) => Все старые имена заменены на современные и понятные.<br>
* Поля структуры Node приведены к корректным именам<br>
* Конструктор очереди переписан<br>
* Исправлена логика добавления элемента (push) => создан новый узел new_node,
корректно обновляется хвост очереди,
удалено лишнее повторное присваивание next = nullptr<br>
* Исправлена логика удаления элемента (pop) => освобождается память, возвращается false, если очередь пуста<br>
* Исправлена функция show() => добавлен вывод сообщения, если очередь пуста<br>
* Исправлена функция even_head() => добавлена проверка на пустую очередь,
корректно извлекает элементы, пока первый не станет чётным,
выводит удалённые значения<br>
* Исправлен деструктор => удаляет все элементы очереди через pop, корректно освобождает память<br>
* Исправлены функции get_p1_data() и get_p2_data()<br>
<br>
2. Обработка ошибок<br>
* Добавлена функция проверки строки на число (is_number)<br>
* Добавлена проверка корректности ввода количества элементов<br>
* Добавлена проверка корректности ввода каждого элемента очереди<br>
* Исправлены циклы ввода => Цикл теперь идёт от 0 до element_count - 1<br>
* Исправлены вызовы методов<br>
* Исправлены строки, превышающие 80 символов<br>
* Добавлена защита от обращения к пустой очереди<br>
* Исправлено освобождение памяти<br>
* Исправлена логика обновления указателей<br>
<br>

<A NAME="chap3"></A>
<H1>Задание 3 </H1>
<A NAME="chap1.3"></A>



<A NAME="chap4"></A>
<H1>Задание 4 </H1>
<A NAME="chap1.4"></A>



