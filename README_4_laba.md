# Лабораторная-4
Вариант - 7 в лабораторной по java 2
<H2>Исправленные ошибки в коде:</H2>
<A HREF="#chap1">Задание 1 </A><br>
<A HREF="#chap2">Задание 2 </A><br>
<A HREF="#chap3">Задание 3 </A><br>

<A NAME="chap1"></A>
<H1>Задание 1 </H1>
<A NAME="chap1.1"></A>
1. Повторяющийся код проверки ввода.<br>
три одинаковых цикла проверки фамилии/имени/отчества.
Исправлено: вынесена общая логика в метод validateInput()<br>
2. Поля становились null, если параметр конструктора был null  <br>
this.lastName = lastName; → могло привести к null в toString().<br>
Исправлено: заменено на this.lastName = (lastName == null ? "" : lastName);<br>
Аналогично для имени и отчества.<br>
3. Отсутствовали сеттеры, хотя по заданию должны быть “свойства” <br>
4. Старая реализация toString() была громоздкой и нарушала Google Style  <br>
Много вложенных if, ручная конкатенация строк.
Исправлено: переписано на StringBuilder с аккуратной логикой добавления частей.<br>
5.Не было обработки пустых строк как “параметр отсутствует”  
пустая строка считалась ошибкой.<br>

<A NAME="chap2"></A>
<H1>Задание 2 </H1>
<A NAME="chap2.1"></A>
1. отсутствовала проверка на нечисловой ввод  <br>
2. неправильная нормализация времени при значениях > 24 часов  <br>
3. отрицательные секунды обрабатывались неверно  <br>
4. Удалена ручная сборка строки в toString()  <br>
5. Удалены лишние вызовы toString() в main.<br>
6. дублирование логики нормализации<br>
7. Конструкторы оформлены по Google Java Style.<br>
8. неверный расчёт часов при больших значениях  <br>
9. лишние вызовы toString() <br>

<A NAME="chap3"></A>
<H1>Задание 3 </H1>
<A NAME="chap3.1"></A>
1. Отсутствие проверки на null при добавлении сотрудника в отдел   добавлена проверка
if (employee == null) { return; }.<br>
2. Дублирование сотрудников в отделе - перед добавлением выполняется проверка в цикле, и при совпадении ссылка не добавляется повторно.<br>
3. Неэффективная конкатенация строк в getAllEmployeesList() - заменено на StringBuilder res = new StringBuilder(...); и res.append(...).<br>
4. Отсутствие проверки отдела на null при создании сотрудника<br>
5. Лишние вызовы toString() при выводе сотрудников  <br>
6. Двойная проверка числа (дублирование логики)  <br>
